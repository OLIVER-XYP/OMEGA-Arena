# Strategy: CONTROL — pure defensive specialist with retaliation.
# Requires competitive_engine.json (DEFEND_RETALIATION_DAMAGE > 0).
# Signature: loaded miners issue 'defend' when a raider is adjacent -> immune AND
# reflect retaliation damage, killing the raider. No hunters, never leaves home.
# Beats: AGGRO (raiders die attacking defended miners) — ~86%.
# Loses to: ECO (deliberately minimal economy can't out-boom a pure economy) — ~75%.
# Cycle: aggro > eco > control > aggro, every edge >66%.
#
# DESIGN NOTE: vs ECO, control never fights (eco has ENABLE_ATTACK=0), so that
# edge is a PURE ECONOMY RACE. To make eco win >66% we deliberately CRIPPLE
# control's economy (the "max_nerf" tune below: hard turtle + minimal mining)
# while keeping the defense intact — defense is per-ship combat, economy-
# independent, so control still wrecks aggro. This trades economic strength for
# a clean RPS cycle.

# Core economy / return timing — minimal economy on purpose
STOP_RATIO=0.040000
SPAWN_END_RATIO=0.280000                     # stop spawning early: small fleet
SAFE_MARGIN=4                               # max turtle: cede the whole open map to ECO
END_RUSH_TURNS=30
RETURN_BUFFER=12
MIN_RETURN_CARGO=220
STRATEGIC_DIST=5
STRATEGIC_RICH=550
STRATEGIC_EFF_THRESH=20.000000
RETURN_CLOSE=0.400000                        # bank early/often: less time mining
RETURN_FAR=0.600000
RETURN_DIST=15

# Mining / target search — very narrow + stingy: mine little, cede econ to ECO
SEARCH_RADIUS_EARLY=6
SEARCH_RADIUS_MID=5
SEARCH_RADIUS_LATE=6
MIN_CELL_HALITE_CONSIDER=100                 # ignore all but rich cells -> mines less
TARGET_TTL=8
STAY_BONUS=1.000000                          # no incentive to fully mine a cell
LONG_DISTANCE_PENALTY=0.020000

# Territorial exploration — pure turtle: never venture out, never harass ECO
INVASION_PENALTY=2.000000
DOMINANCE_RELAX_RATIO=9.900000
DOMINANCE_DISABLE_RATIO=9.900000
EXPAND_RADIUS_BONUS=0

# Navigation / congestion
ENABLE_SECONDARY_NAV=1
CONGESTION_RADIUS=2
CONGESTION_PENALTY=4
ALLOW_SWAP_LIKE_ESCAPE=1

# Spawn timing — balanced
ENABLE_SPAWN_ROI=0
SPAWN_MIN_TURNS_LEFT=90
SPAWN_ROI_MIN=1.100000
SPAWN_PAYBACK_TURNS=70
SPAWN_CUTOFF_TURN_RATIO=0.120000
SHIPYARD_TARGET_PENALTY=1.200000
STUCK_RESET_TURNS=3

# Dropoff construction — single base only: minimal map coverage (cedes econ to ECO)
ENABLE_DROPOFF=1
MAX_DROPOFFS=1
DROPOFF_MIN_TURN=80
DROPOFF_MAX_TURN=780
DROPOFF_MIN_DIST_FROM_OWN=10
DROPOFF_SITE_RADIUS=4
DROPOFF_MIN_LOCAL_HALITE=6000
DROPOFF_MIN_NEARBY_SHIPS=3
DROPOFF_MIN_PLAYER_HALITE_BUFFER=1200
DROPOFF_EMERGENCY_RETURN_TURNS=80

# Combat / attack — opportunistic only; defense (below) is the real weapon
ENABLE_ATTACK=1
ATTACK_ONLY_WHEN_ENABLED_BY_RULES=1
ATTACK_RATIO=1.300000
ATTACK_MIN_TARGET_HALITE=120
ATTACK_MAX_SELF_HALITE_FOR_RISK=700
ATTACK_STRUCTURE_VALUE=280
ATTACK_SHIP_VALUE=250

# Hunt mode — DISABLED: control never chases (would lose its home heal + invite losses)
HUNT_MIN_ENEMY_HALITE=150
HUNT_MAX_RANGE=10
HUNT_MAX_HUNTERS=2
HUNT_MIN_TURN=50
LOW_HP_RETURN=35
ATTACK_MIN_SELF_HP=45

# Defense: loaded miners issue 'defend' (immune + retaliate via engine
# DEFEND_RETALIATION_DAMAGE) when a raider is adjacent.
DEFEND_MIN_CARGO=300                         # balance sweep raised 200→300: fewer
                                             # ships qualify to defend, pulling the
                                             # control>aggro ceiling 90→84% down to the
                                             # eco>control anchor. Past 300 it craters
                                             # eco>control too (defend gates the econ race).
DEFEND_MIN_CELL_HALITE=100
DEFEND_TRIGGER_RANGE=4
FOCUS_FIRE_MIN_SHIPS=0
# Threat-aware banking: route loaded ships home AROUND raiders, not through them.
# (Best-balance lever — Nash 88%. The spawn-guard SPAWN_STOP_IF_BEHIND_BY was
# tested and left OFF: it preserved treasury but a smaller fleet hurt more.)
THREAT_AVOID_WEIGHT=3.0

# Logging / diagnostics
ENABLE_PERIODIC_LOG=0
LOG_PERIOD=20
